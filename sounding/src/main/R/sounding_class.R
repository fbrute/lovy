## une classe pour modéliser un sounding

Sounding <- seRefClass("Sounding",
    fields = list (
        id = "numeric",
        station_name = "character",
        station_number = "numeric"
    ) 
                      
            )